# Build output
out/
dist/
.vscode-test/

# docs/ is a standalone Next.js site with its own build output and lockfile
docs/.next/

# pnpm rewrites lockfiles; formatting them would be undone on the next install
pnpm-lock.yaml
docs/pnpm-lock.yaml

# Packaged extension
*.vsix

# Stray directory left by the packaging tooling
package/
